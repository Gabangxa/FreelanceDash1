This Flask/SQLAlchemy project has two parallel DB migration mechanisms that can
  silently diverge:                                                                                                                                                                                                                           
    - Alembic (Flask-Migrate) in `migrations/`                                                                                                                                                                                                
    - Inline `db.create_all()` + raw `ALTER TABLE` blocks in `app.py` (lines ~407–545)                                                                                                                                                        
                                                                                                                                                                                                                                              
  Consolidate everything into Alembic only.                                                                                                                                                                                                   
                                                                                                                                                                                                                                              
  Steps:          
  1. Read `app.py` lines 400–560. Extract every `ALTER TABLE` statement and the
     columns/defaults they add. List them with their target table and column name.                                                                                                                                                            
  2. Read `migrations/versions/` to find the latest revision ID.                                                                                                                                                                              
  3. Generate a new Alembic migration file `migrations/versions/0007_consolidate_startup_alters.py`                                                                                                                                           
     that:                                                                                                                                                                                                                                    
     - In `upgrade()`: applies each ALTER as an `op.add_column` / `op.alter_column`                                                                                                                                                           
       wrapped in a guard: check `op.get_bind().dialect.has_table(...)` and                                                                                                                                                                   
       `inspector.get_columns(table)` so it is idempotent on an already-patched DB.                                                                                                                                                           
     - In `downgrade()`: drops those columns with `op.drop_column`.                                                                                                                                                                           
  4. In `app.py`, remove the `db.create_all()` call and all raw `ALTER TABLE` blocks                                                                                                                                                          
     that were just migrated. Replace with a single comment:                                                                                                                                                                                  
     `# Schema managed exclusively by Alembic — run: flask db upgrade`                                                                                                                                                                        
  5. Do NOT touch any other app logic in `app.py`.                                                                                                                                                                                            
  6. Add a check to `app.py` startup (or a `@app.before_first_request` equivalent)                                                                                                                                                            
     that warns to stderr if Alembic `current()` != `head`, so the operator knows                                                                                                                                                             
     to run migrations before serving traffic.                                                                                                                                                                                                
  7. Run `flask db upgrade` in the terminal to verify the new migration applies                                                                                                                                                               
     cleanly on the local DB, then confirm `flask db current` shows `0007 (head)`.                                                                                                                                                            
                                                                                                                                                                                                                                              
  Do not auto-run destructive SQL. Show me the generated migration file before writing it.                                                                                                                                                    
                                                                                           